# 全局实施计划复核（PL-01）

- 日期：2026-09-25
- 基线：`500cde2`；审查范围：`docs/STATUS.md`、`docs/IMPLEMENTATION_PLAN.md`、`docs/PROJECT_PLAN.md`、`docs/ARCHITECTURE.md` 与 Stage 2 审查记录
- 结论：规划顺序与事实口径需要调整；调整后仍由 S2-07C 承担当前工程审查，Stage 2 未退出。

| 发现 | 调整 | 仍需后续验证 |
| --- | --- | --- |
| 90% 全 App 对齐基线原安排在 S4-09 才冻结，Stage 3 容易在缺少对照目标时实现并返工 | 新增 S3-00，在 Stage 3 扩展前冻结上游版本、适用清单、权重、截图与差距；S4-09 改为最终复核和补差 | S3-00 尚未执行，当前不能报告任何对齐百分比 |
| Stage 3/4 任务缺少状态、依赖和具体验收，S4-07 发布准备排在对齐验收之前 | 补齐依赖、TODO 状态和关键验收；S4-07 必须在 S4-09 后完成 | 各大任务开始时还需按可独立编译/提交的切片细化 |
| 原 90% 文案提到权重，却以未加权的“通过项占比”计算，可能隐藏关键闭环缺失 | 明确加权公式、未验证项计分和平台排除条件；主要阅读与离线闭环设为不可被总分抵消的强制验收 | S3-00 需在实施前固定具体权重与截图评审表 |
| 产品蓝图保留 Stage 0、Hilt、JavaScriptEngine 和独立下载/本地 Feature 等过期现状表述 | 把实时阶段交给 STATUS；当前装配、QuickJS 与 Library tabs 改为事实描述；明确 QuickJS 只有会话隔离，没有 OS 进程隔离 | 模块表中其余未创建模块仍是目标，不可据此提前建空模块 |
| Stage 2 设备闭环未通过，用户要求确认后才运行真机 | 新增 S2-07D 为 BLOCKED，列出解除条件；S2-07C 只继续本地审查，Stage 2 保持 IN_PROGRESS | 设备/人工验收等待用户确认；不得把编译或 JVM 测试算作设备通过 |

本轮仅调整规划和记录，未运行真机、未执行代码构建。验证：`git diff --check` 与暂存差异检查。代码质量审查另行记录到 Stage 2 审查台账，并将每个修复拆成独立提交。
